export const DEFAULT_PORTS = {
  apiGateway: 4001,
  modelOrchestrator: 4002,
} as const;

export const SERVICE_NAMES = {
  apiGateway: "api-gateway",
  modelOrchestrator: "model-orchestrator",
} as const;
